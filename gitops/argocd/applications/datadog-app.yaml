apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: datadog
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: monitoring
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: monitoring

  sources:
    - repoURL: https://helm.datadoghq.com
      chart: datadog
      targetRevision: "3.67.0"
      helm:
        valueFiles:
          - $values/gitops/apps/monitoring/datadog/values.yaml

    - repoURL: https://github.com/Arthur-Rosa/FIAP-TCs-Challenge-DevOps
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
